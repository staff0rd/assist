- id: 1
  type: story
  name: add radon to block python complexity
  acceptanceCriteria: []
  status: done
- id: 2
  type: story
  name: add new alias for assist
  description: install of the assist program should add a new alias `ast` both the `assist` alias and the new alias `ast` should be supported
  acceptanceCriteria:
    - i can start the program with ast
    - i can start the program with assist
  status: done
- id: 3
  type: story
  name: jb inspect
  description: |-
    we can use `jb inspect` to find dead c# code in the files we've changed:

    ```
      dotnet tool install -g JetBrains.ReSharper.GlobalTools
      jb inspectcode YourSolution.sln -o=report.xml --include="src/Path/To/ChangedFile.cs"
    ```

    We should add this as a dotnet command to assist.
  acceptanceCriteria: []
  status: done
- id: 4
  type: story
  name: Add refactor extract command
  description: Add assist refactor extract <file> <functionName> <destination> that uses ts-morph to move a function and its private dependencies from one file to a new file, updating all imports. Follows existing refactor patterns (dry-run by default, --apply to execute).
  acceptanceCriteria:
    - Extracts the named function and all same-file private (non-exported) functions it transitively depends on
    - Creates the destination file with the extracted functions and their required imports
    - Removes the extracted functions from the source file
    - Adds import { functionName } from "./destination" to the source file
    - Updates any other files that already import the function (if it was exported)
    - Adds the export to the nearest barrel index.ts if one exists in the destination directory
    - Dry-run by default showing what would happen, --apply to execute
    - assist verify passes after extraction with --apply
  plan:
    - name: Core extraction logic
      tasks:
        - task: Find the target function declaration in the source file using ts-morph
          verify: Function is located by name and type
        - task: Walk the same-file call graph to collect all private (non-exported) functions the target transitively depends on
          verify: All transitive private deps are identified
        - task: Resolve the required imports for the destination file (imports used by extracted functions)
          verify: Import list is complete and correct
    - name: File manipulation
      tasks:
        - task: Create the destination file with extracted functions and their required imports
          verify: Destination file compiles independently
        - task: Remove extracted functions from the source file
          verify: Source file still compiles
        - task: Add import of the extracted function to the source file
          verify: Source file references resolve
        - task: Update any other files that already import the function
          verify: No broken imports across project
        - task: Add export to nearest barrel index.ts if one exists in the destination directory
          verify: Barrel file exports the new function
    - name: Registration and wiring
      tasks:
        - task: Register the extract command under refactor with --apply flag
          verify: assist refactor extract --help works
        - task: Update README.md with new command documentation
        - task: Update CLAUDE.md refactor docs to mention extract command
        - task: Add read-only command to settings.json allow list
  status: done
- id: 5
  type: story
  name: Display plan and phase progress in backlog web view
  description: The backlog web detail view doesn't render the plan, currentPhase, or manualChecks fields that were recently added to the schema. Add a read-only plan section below the acceptance criteria showing phases, tasks, verify steps, and manual checks, with the current phase visually indicated.
  acceptanceCriteria:
    - Web UI BacklogItem type includes plan, currentPhase, and manualChecks
    - ItemBody renders a Plan section below acceptance criteria when plan is present
    - Each phase shows its name, tasks (with optional verify steps), and manual checks
    - The current phase (based on currentPhase) is visually highlighted; completed phases are marked as done
    - Items with no plan show no plan section (no empty state)
  plan:
    - name: Update types and plan component
      tasks:
        - task: Update the web UI BacklogItem type to match the core schema (add plan, currentPhase, manualChecks)
          verify: Type compiles and matches core BacklogItem
        - task: Create a read-only PlanSection component that renders phases, tasks, verify steps, and manual checks with current/completed phase indicators
          verify: Component renders correctly with sample data
    - name: Wire into detail view
      tasks:
        - task: Render PlanSection in ItemBody below acceptance criteria
          verify: Plan section appears in the web detail view for items with a plan
        - task: Verify items with no plan show no plan section
          verify: Detail view for plan-less items renders without errors or empty sections
  currentPhase: 2
  status: done
- id: 6
  type: story
  name: Auto-generate review phase for backlog items
  description: Automatically append a review phase to every backlog item when added. The review phase verifies all acceptance criteria against the implementation, asks the user to perform manual checks, marks the story done, calls /commit, then signals phase-done.
  acceptanceCriteria:
    - When a backlog item with a plan is added (via --json or interactive), a review phase is auto-appended
    - The review phase prompt instructs the agent to verify each acceptance criterion against the current code
    - The agent reports pass/fail per criterion before proceeding
    - The agent asks the user to confirm manual checks
    - After confirmation, the agent runs assist backlog done <id>, then /commit, then assist backlog phase-done
    - The review phase is always the last phase and is not editable by the author
    - Existing items without a review phase are unaffected (no migration)
  plan:
    - name: Auto-append review phase on add
      tasks:
        - task: "Create a helper function that generates the review phase object (name: Review, with a single task describing the review process)"
          verify: Helper returns a valid PlanPhase
        - task: Modify addFromJson in add/index.ts to append the review phase to items that have a plan
          verify: JSON-added items with a plan get the review phase appended
        - task: Interactive add does not add plans so no change needed there — add a comment clarifying this
          verify: Interactive add still works unchanged
    - name: Review phase prompt
      tasks:
        - task: Add a detection mechanism in buildPhasePrompt to identify the review phase (e.g. by name convention)
          verify: Review phase is correctly identified
        - task: Build a specialised prompt for the review phase that lists each AC and instructs the agent to verify against code
          verify: Prompt includes all acceptance criteria with verify instructions
        - task: "Prompt instructs the agent to: confirm manual checks, run assist backlog done, call /commit, then call phase-done"
          verify: Prompt contains the correct command sequence
    - name: Wiring and docs
      tasks:
        - task: Update run.ts completion message since the review phase now handles commit
          verify: No duplicate commit suggestion after review phase
        - task: Update README.md with documentation about the auto-generated review phase
          verify: README mentions the review phase behaviour
  currentPhase: 3
  status: done
- id: 7
  type: story
  name: Number acceptance criteria for easy reference
  description: Change acceptance criteria rendering from bullet points to numbered lists so individual criteria can be referenced by number (e.g. AC 1).
  acceptanceCriteria:
    - CLI backlog list -v shows numbered AC (e.g. 1. criterion)
    - Phase prompt sent to Claude uses numbered AC
    - Web UI renders AC as a numbered list
  plan:
    - name: Update rendering
      tasks:
        - task: In buildPhasePrompt.ts, change .map((c) => `- ${c}`) to use 1-based index numbering
          verify: assist backlog list -v shows numbered AC
        - task: In list/shared.ts, change the console.log bullet to use 1-based index numbering
        - task: In AcceptanceCriteriaList.tsx, replace bullet span with 1-based index number and change ul to ol
  currentPhase: 2
  status: done
- id: 8
  type: story
  name: Refactor isApprovedRead tests to BDD style
  description: Restructure isApprovedRead.test.ts to follow BDD conventions with AAA pattern, when/should phrasing, and add missing coverage.
  acceptanceCriteria:
    - All describe blocks use when phrasing (e.g. describe("when the command is cd"))
    - All it blocks use should phrasing (e.g. it("should return a reason"))
    - Each test follows Arrange, Act, Assert with whitespace separation (no comments)
    - Each test has at most two closely related assertions
    - "Missing edge cases added: cd with trailing slash, cd with unusual whitespace"
    - Tests pass and /verify is green
  plan:
    - name: Restructure and extend tests
      tasks:
        - task: Rewrite describe blocks to use when phrasing grouped by scenario
          verify: npx vitest run src/shared/isApprovedRead.test.ts
        - task: Rewrite it blocks to use should phrasing
          verify: npx vitest run src/shared/isApprovedRead.test.ts
        - task: Separate each test into Arrange, Act, Assert with whitespace
          verify: npx vitest run src/shared/isApprovedRead.test.ts
        - task: Add tests for cd with trailing slash and unusual whitespace
          verify: npx vitest run src/shared/isApprovedRead.test.ts
      manualChecks:
        - Review test file reads naturally as a behaviour specification
  currentPhase: 2
  status: done
- id: 9
  type: story
  name: Refactor isGhApiRead tests to BDD style and add missing coverage
  description: Restructure isGhApiRead.test.ts to follow BDD conventions and fill coverage gaps for -X=GET equals syntax, GraphQL with explicit method, and --field flag variant.
  acceptanceCriteria:
    - All describe blocks use when phrasing
    - All it blocks use should phrasing
    - Each test follows AAA pattern with whitespace separation
    - Add test for -X=GET equals syntax
    - Add test for GraphQL with explicit --method GET
    - Add test for --field flag variant in extractGraphqlQuery path
    - Tests pass and /verify is green
  plan:
    - name: Restructure and extend tests
      tasks:
        - task: Rewrite describe/it blocks to BDD when/should phrasing
          verify: npx vitest run src/shared/isGhApiRead.test.ts
        - task: Separate each test into Arrange, Act, Assert with whitespace
          verify: npx vitest run src/shared/isGhApiRead.test.ts
        - task: Add test for -X=GET equals syntax
          verify: npx vitest run src/shared/isGhApiRead.test.ts
        - task: Add test for GraphQL with explicit --method GET
          verify: npx vitest run src/shared/isGhApiRead.test.ts
        - task: Add test for --field flag variant in extractGraphqlQuery
          verify: npx vitest run src/shared/isGhApiRead.test.ts
      manualChecks:
        - Review test file reads naturally as a behaviour specification
  currentPhase: 2
  status: done
- id: 10
  type: story
  name: Refactor splitCompound tests to BDD style
  description: Restructure splitCompound.test.ts to follow BDD conventions with when/should phrasing and AAA pattern.
  acceptanceCriteria:
    - All describe blocks use when phrasing (e.g. describe("when the command contains pipes"))
    - All it blocks use should phrasing
    - Each test follows AAA pattern with whitespace separation
    - /test-review passes with no BDD structure issues
    - Tests pass and /verify is green
  plan:
    - name: Restructure tests
      tasks:
        - task: Rewrite describe blocks to use when phrasing grouped by scenario
          verify: npx vitest run src/shared/splitCompound.test.ts
        - task: Rewrite it blocks to use should phrasing
          verify: npx vitest run src/shared/splitCompound.test.ts
        - task: Separate each test into Arrange, Act, Assert with whitespace
          verify: npx vitest run src/shared/splitCompound.test.ts
      manualChecks:
        - Review test file reads naturally as a behaviour specification
  currentPhase: 2
  status: done
- id: 11
  type: story
  name: Refactor findFunction tests to BDD style
  description: Restructure findFunction.test.ts to follow BDD conventions. Split tests with multiple assertions into separate it blocks.
  acceptanceCriteria:
    - All describe blocks use when phrasing
    - All it blocks use should phrasing
    - Each test follows AAA pattern with whitespace separation
    - Tests with two assertions split into separate it blocks under same describe(when)
    - /test-review passes with no BDD structure issues
    - Tests pass and /verify is green
  plan:
    - name: Restructure tests
      tasks:
        - task: Rewrite describe/it blocks to BDD when/should phrasing
          verify: npx vitest run src/commands/refactor/extract/findFunction.test.ts
        - task: Split tests with multiple assertions (e.g. toBeDefined + getName) into separate it blocks
          verify: npx vitest run src/commands/refactor/extract/findFunction.test.ts
        - task: Separate each test into Arrange, Act, Assert with whitespace
          verify: npx vitest run src/commands/refactor/extract/findFunction.test.ts
      manualChecks:
        - Review test file reads naturally as a behaviour specification
  currentPhase: 1
  status: done
- id: 12
  type: story
  name: Refactor collectDependencies tests to BDD style
  description: Restructure collectDependencies.test.ts to follow BDD conventions. Split tests with multiple assertions and group by scenario.
  acceptanceCriteria:
    - All describe blocks use when phrasing
    - All it blocks use should phrasing
    - Each test follows AAA pattern with whitespace separation
    - Tests with two assertions (e.g. variables.length + getText) split into separate it blocks
    - /test-review passes with no BDD structure issues
    - Tests pass and /verify is green
  plan:
    - name: Restructure tests
      tasks:
        - task: Rewrite describe/it blocks to BDD when/should phrasing, grouping by scenario
          verify: npx vitest run src/commands/refactor/extract/collectDependencies.test.ts
        - task: Split tests with multiple assertions into separate it blocks under same describe(when)
          verify: npx vitest run src/commands/refactor/extract/collectDependencies.test.ts
        - task: Separate each test into Arrange, Act, Assert with whitespace
          verify: npx vitest run src/commands/refactor/extract/collectDependencies.test.ts
      manualChecks:
        - Review test file reads naturally as a behaviour specification
  currentPhase: 2
  status: done
- id: 13
  type: story
  name: Refactor resolveImports tests to BDD style
  description: Restructure resolveImports.test.ts to follow BDD conventions with when/should phrasing and AAA pattern.
  acceptanceCriteria:
    - All describe blocks use when phrasing
    - All it blocks use should phrasing
    - Each test follows AAA pattern with whitespace separation
    - /test-review passes with no BDD structure issues
    - Tests pass and /verify is green
  plan:
    - name: Restructure tests
      tasks:
        - task: Rewrite describe/it blocks to BDD when/should phrasing, grouping by scenario
          verify: npx vitest run src/commands/refactor/extract/resolveImports.test.ts
        - task: Separate each test into Arrange, Act, Assert with whitespace
          verify: npx vitest run src/commands/refactor/extract/resolveImports.test.ts
      manualChecks:
        - Review test file reads naturally as a behaviour specification
  currentPhase: 2
  status: done
- id: 14
  type: bug
  name: refactor extract leaves unused imports in source file
  description: |-
    **Repro:**
    1. Have a file (`run.ts`) with a function (`resolvePlan`) that is the sole consumer of certain type imports (`BacklogItem`, `PlanPhase`)
    2. Run `assist refactor extract src/commands/backlog/run.ts resolvePlan src/commands/backlog/resolvePlan.ts --apply`
    3. Inspect the source file after extraction

    **Expected:** Imports that are no longer used by any remaining code in the source file are removed during extraction.

    **Actual:** The `import type { BacklogItem, PlanPhase } from "./types"` was left behind in `run.ts` despite `resolvePlan` being the only function that referenced those types. This caused a lint failure (`noUnusedImports`).
  acceptanceCriteria:
    - After extracting a function, any imports in the source file that are no longer referenced by remaining code are removed
    - Extraction does not remove imports that are still used by other code in the source file
  currentPhase: 2
  status: done
- id: 15
  type: bug
  name: backlog add --json fails when echo passes JSON with \n literals
  description: |-
    **Repro:**
    1. An agent generates JSON with `\n` escape sequences in string values
    2. Pipes it to `assist backlog add --json` using `echo`
    3. `JSON.parse` throws `SyntaxError: Bad control character in string literal`

    **Expected:** The agent successfully adds a backlog item on the first attempt.

    **Actual:** The command fails, requiring the agent to retry with a different shell approach (heredoc).
  acceptanceCriteria:
    - An agent can add a backlog item with multi-line description fields in a single attempt without hitting a JSON parse error
  currentPhase: 2
  status: done
- id: 16
  type: bug
  name: refactor extract adds unused barrel export to index.ts
  description: |-
    **Repro:**
    1. Have a file with a private helper function (e.g. `handleIncompletePhase` in `resolvePhaseResult.ts`) that is only used internally within the same directory
    2. Run `assist refactor extract <source> <functionName> <destination> --apply`
    3. Check the directory's `index.ts` barrel file

    **Expected:** The extracted function should only be exported from the new file and imported where needed. No barrel export should be added unless the function was already publicly exported.

    **Actual:** The extract command unconditionally adds a barrel export to `index.ts` (e.g. `export { handleIncompletePhase } from "./handleIncompletePhase"`), even when the function is only consumed internally. This causes knip to flag an unused export.
  acceptanceCriteria:
    - refactor extract only adds a barrel export when the function was already exported via the barrel before extraction
    - Extracting a non-barrel-exported function does not modify index.ts
  currentPhase: 1
  status: done
- id: 17
  type: bug
  name: refactor extract copies import paths verbatim into destination
  description: |-
    **Repro:**
    1. Have a file `src/commands/registerBacklog.ts` that imports from `"./backlog"` (a barrel)
    2. Run `assist refactor extract src/commands/registerBacklog.ts registerItemCommands src/commands/backlog/registerItemCommands.ts --apply`
    3. The extracted file lands inside `backlog/` but still contains `from "./backlog"`

    **Expected:** Import paths in the extracted file are rewritten relative to the destination directory (e.g. direct imports from `./add`, `./init`, `./list`).

    **Actual:** Import paths are copied verbatim from the source file, producing an unresolvable `./backlog` import that fails build and type checks.
  acceptanceCriteria:
    - When extracting to a different directory, import paths in the destination file are rewritten relative to the destination
    - Build and type checks pass on the extracted file without manual fixup
  currentPhase: 1
  status: done
- id: 18
  type: bug
  name: Fix cli-hook check failing to parse glob patterns
  description: 'splitCompound rejects commands containing glob patterns (e.g. *.ts) because shell-quote parses them as { op: "glob", pattern: "..." } tokens, which are not strings. These should be treated as safe argument values.'
  acceptanceCriteria:
    - assist cli-hook check with glob patterns (e.g. *.ts) no longer returns unable to parse
    - Glob tokens are converted to their pattern string in splitCompound
    - Existing tests still pass; new test covers glob handling
  plan:
    - name: Fix glob handling
      tasks:
        - task: Update groupByOperator in splitCompound.ts to treat glob tokens as their pattern string
          verify: assist test splitCompound
        - task: Add test cases for glob patterns in splitCompound tests
          verify: assist test splitCompound
  currentPhase: 1
  status: done
- id: 19
  type: bug
  name: Fix run add to split inline command args at save time
  description: assist run add stores a quoted multi-word command as a single string with no args. assist verify then fails because buildFullCommand in resolveEntries.ts does not split at runtime. Fix by splitting at save time in add.ts, unifying with the split logic already in setupVerifyRunEntry.ts.
  acceptanceCriteria:
    - 'assist run add verify:lint "npx biome check --diagnostic-level=error" stores command: npx and args: [biome, check, --diagnostic-level=error]'
    - setupVerifyRunEntry.ts uses the same shared buildRunEntry
    - Runtime split workaround removed from buildCommand in index.ts
    - "Tests cover: no-space command, space-in-command split, and separate args passthrough"
  plan:
    - name: Extract and fix buildRunEntry
      tasks:
        - task: Extract buildRunEntry to src/commands/run/buildRunEntry.ts with whitespace splitting when args is empty
          verify: npx vitest run src/commands/run/buildRunEntry.test.ts
        - task: Update add.ts to import from the new file
          verify: npx vitest run src/commands/run/buildRunEntry.test.ts
        - task: Update setupVerifyRunEntry.ts to use the shared function, adding GLOBAL_COMMANDS/npx logic on top
          verify: npx vitest run src/commands/verify
        - task: Remove the runtime split workaround from buildCommand in index.ts
          verify: npx vitest run src/commands/run
    - name: Tests
      tasks:
        - task: Add src/commands/run/buildRunEntry.test.ts covering no-split, split, and separate-args cases
          verify: npx vitest run src/commands/run/buildRunEntry.test.ts
  currentPhase: 2
  status: done
- id: 20
  type: story
  name: Add --dir option to backlog commands
  description: Add a --dir option to the backlog parent command that overrides the directory used for backlog file discovery and marker file resolution. When provided, all backlog operations resolve assist.backlog.yml and .assist-phase-status.json from the given directory instead of process.cwd(). Config discovery is unaffected.
  acceptanceCriteria:
    - backlog --dir /some/path list reads the backlog from /some/path/assist.backlog.yml
    - backlog --dir /some/path add --json writes to /some/path/assist.backlog.yml
    - backlog --dir /some/path run <id> writes the phase status marker to /some/path/.assist-phase-status.json
    - When --dir is not provided, behavior is unchanged (uses cwd)
    - The top-level next alias also respects --dir if passed
  plan:
    - name: Plumb --dir through backlog commands
      tasks:
        - task: Add --dir option to the backlog parent command in registerBacklog.ts
          verify: assist backlog --help shows --dir option
        - task: Refactor getBacklogPath() and getPhaseStatusPath() to accept an optional directory parameter and thread it through all callers (shared helpers, web, phaseDone, etc.)
          verify: assist backlog --dir /tmp list runs without error
    - name: Update next alias and docs
      tasks:
        - task: Wire --dir through the top-level next alias
          verify: assist next --help shows --dir option
        - task: Update README.md with the new --dir option
          verify: grep --dir README.md
  status: todo
- id: 21
  type: story
  name: Auto-commit backlog after adding an item
  description: After backlog add saves the YAML file, automatically git add and git commit the backlog file with a descriptive commit message including the item ID and name.
  acceptanceCriteria:
    - "After backlog add, the backlog file is committed with message chore: add backlog item #<id> — <name>"
    - If the repo has no git or the commit fails (e.g. dirty index conflicts), the item is still added — the commit is best-effort with a warning
    - Interactive and --json add paths both trigger the commit
  plan:
    - name: Add auto-commit after backlog add
      tasks:
        - task: "After saveBacklog() in add/index.ts, git add the backlog file and commit it with message chore: add backlog item #<id> — <name>. Handle errors gracefully (warn but do not fail). Ensure both the interactive and --json code paths are covered."
          verify: echo {"name":"test item","type":"story","acceptanceCriteria":["works"],"plan":[]} | assist backlog add --json && git log -1 --oneline | grep backlog
  currentPhase: 1
  status: done
- id: 22
  type: story
  name: Add comments and phase summaries to backlog items
  description: Allow agents to post comments against backlog items at any time for findings or changes worth noting, and require a concise phase summary when completing a phase. Both are stored as annotations on the item, visible via CLI, and available for other phases to read on demand.
  acceptanceCriteria:
    - assist backlog comment <id> <text> adds a comment to the item
    - assist backlog phase-done <id> <phase> <summary> accepts a required summary argument
    - "Comments are stored in a comments array on the backlog item with { text, phase?, timestamp, type: summary | comment }"
    - assist backlog comments <id> lists all comments/summaries for an item
    - Phase prompts tell the agent it can run assist backlog comments <id> to read prior phase notes, and that it can run assist backlog comment <id> <text> to post findings or changes
    - Phase prompts instruct the agent to provide a summary when calling phase-done
    - "`assist backlog web` displays comments/summaries on the item detail screen"
    - "`assist backlog show <id>` (#23) displays comments if present — update show command to render comments when this lands first"
  plan:
    - name: Schema and storage
      tasks:
        - task: "Add comments array to backlog item schema in types.ts with { text, phase?, timestamp, type: summary | comment }"
          verify: assist verify
        - task: Add helper functions to append a comment/summary entry in shared.ts
          verify: assist verify
    - name: Commands
      tasks:
        - task: Add backlog comment <id> <text> command that appends a comment entry
          verify: assist backlog comment 1 "test comment" works
        - task: Update phase-done to accept and store a required summary argument
          verify: assist verify
        - task: Add backlog comments <id> list command that displays all comments/summaries
          verify: assist backlog comments 1 shows entries
        - task: Register all new commands in registerBacklog.ts
          verify: assist verify
    - name: Web view
      tasks:
        - task: Update the backlog web item detail screen to display comments/summaries
          verify: assist verify
    - name: Prompt integration
      tasks:
        - task: Update buildAuthoredPhasePrompt.ts to inform the agent about comment and comments commands and that it can read prior comments
          verify: assist verify
        - task: Update buildReviewPrompt.ts to inform the agent about comment and comments commands
          verify: assist verify
  currentPhase: 4
  status: done
  comments:
    - text: Added comment, comments, and phase-done summary commands. Registered in registerBacklog.ts, updated show to display comments, extracted formatComment helper to avoid duplication, split registerBacklog into smaller functions for maintainability.
      phase: 1
      timestamp: 2026-03-30T04:30:08.701Z
      type: summary
    - text: Added CommentsSection component to web UI. Updated UI types to include BacklogComment, created CommentsSection.tsx with styled cards for comments and summaries (amber for summaries, gray for comments), and wired it into ItemBody after the plan section.
      phase: 2
      timestamp: 2026-03-30T04:31:38.671Z
      type: summary
- id: 23
  type: story
  name: Add backlog show command
  description: "Add a `backlog show <id>` command (alias `view`) that displays all detail for a backlog item in formatted terminal output — name, type, status, description, acceptance criteria, plan with phases/tasks/verify hints, and comments (if the comments feature from #22 is available)."
  acceptanceCriteria:
    - "`assist backlog show <id>` prints full item detail: id, name, type, status, description, acceptance criteria (numbered), and plan (phases, tasks, verify hints, manual checks)"
    - "`assist backlog view <id>` works as an alias for `show`"
    - "If the item has comments (per #22), they are displayed with timestamp, phase, and type"
    - Exits with a clear error if the item ID does not exist
    - Current phase is visually indicated in the plan output
  plan:
    - name: Implement show command
      tasks:
        - task: Create `src/commands/backlog/show/index.ts` with formatted output for all item fields (id, name, type, status, description, numbered acceptance criteria, plan with phases/tasks/verify hints/manual checks, current phase indicator). Handle missing optional fields (description, plan, comments) gracefully.
          verify: assist backlog show <id> displays full detail for an existing item
        - task: Register `show` subcommand (alias `view`) in `registerBacklog.ts`
          verify: assist backlog view <id> works identically to assist backlog show <id>
        - task: Exit with a clear error message when the item ID does not exist
          verify: assist backlog show 9999 prints an error and exits non-zero
    - name: README and settings
      tasks:
        - task: Add `backlog show` / `backlog view` to README command documentation
          verify: grep for backlog show in README.md
        - task: Add `backlog show` and `backlog view` to the read-only allow list in `claude/settings.json`
          verify: grep for backlog show in claude/settings.json
  currentPhase: 2
  status: done
- id: 24
  type: story
  name: Add "a" as CLI alias
  description: Register "a" as an additional bin alias for the assist CLI, matching the existing "ast" pattern.
  acceptanceCriteria:
    - '"a" is listed in package.json bin field pointing to ./dist/index.js'
  plan:
    - name: Add alias
      tasks:
        - task: Add "a" entry to the bin field in package.json
          verify: grep '"a":' package.json
  currentPhase: 1
  status: done
  comments:
    - text: Added "a" as bin alias in package.json pointing to ./dist/index.js
      phase: 0
      timestamp: 2026-03-30T06:54:02.633Z
      type: summary
- id: 25
  type: story
  name: Inline backlog comments in phase prompts
  description: Embed existing comments directly into the phase prompt text instead of instructing the agent to read them via CLI. Remove the read-comments instruction but keep the write-comments instruction.
  acceptanceCriteria:
    - "When a backlog item has comments, they appear in the prompt after the acceptance criteria in a Comments: section"
    - The read-comments CLI instruction is removed from both authored and review phase prompts
    - The write-comments CLI instruction is still present in both prompts
    - "When a backlog item has no comments, no Comments: section appears in the prompt"
  plan:
    - name: Inline comments
      tasks:
        - task: Update buildAuthoredPhasePrompt.ts and buildReviewPrompt.ts to embed comments after AC section, remove the read-comments line, and keep the write-comments line. Use formatComment or similar to render each comment inline.
          verify: assist verify
  status: todo
- id: 26
  type: story
  name: CLI launch modes for draft and bug with /next chaining
  description: Add assist draft and assist bug CLI commands that spawn Claude Code in the respective slash command mode, watching for a generic signal file. Introduce a /next slash command that writes the signal to terminate the session and chain into assist next. Rename .assist-phase-status.json to a generic signal file used for all assist-Claude communication.
  acceptanceCriteria:
    - .assist-phase-status.json is renamed to a generic signal file (e.g. .assist-signal.json) with an event-type field
    - Existing phase-done flow works as before using the new file format
    - assist draft spawns claude /draft with default permissions, watching for the signal file
    - assist bug spawns claude /bug with default permissions, watching for the signal file
    - /next slash command writes the signal file, causing the watcher to terminate Claude
    - After termination via /next, assist immediately runs assist next (the full backlog-next flow)
    - Signal file is cleaned up between sessions
  plan:
    - name: Rename signal file
      tasks:
        - task: Rename .assist-phase-status.json to a generic signal file name (e.g. .assist-signal.json) with an event-type field in the JSON payload
          verify: grep for old filename across codebase returns no results
        - task: Update phaseDone to write the new file format with event type
          verify: assist backlog phase-done still writes the file correctly
        - task: Update watchForMarker and resolvePhaseResult to read the new file format
          verify: existing phase-done detection logic works with new format
        - task: Update .gitignore and any other references to the old filename
          verify: grep for assist-phase-status returns no results
    - name: Add /next command and CLI launch modes
      tasks:
        - task: Create /next slash command that writes the generic signal file with a next event type
          verify: running /next from Claude writes the signal file
        - task: Add assist draft CLI command that spawns claude /draft with default permissions and watches for the signal file
          verify: assist draft launches Claude in /draft mode
        - task: Add assist bug CLI command that spawns claude /bug with default permissions and watches for the signal file
          verify: assist bug launches Claude in /bug mode
        - task: On signal detection, terminate Claude and chain into assist next
          verify: after /next signal, Claude is terminated and assist next runs
  currentPhase: 2
  status: in-progress
  comments:
    - text: "Renamed .assist-phase-status.json to .assist-signal.json with event-type field. Updated phaseDone to write event: phase-done, renamed getPhaseStatusPath to getSignalPath, updated watchForMarker, resolvePhaseResult, .gitignore, README, and tests."
      phase: 0
      timestamp: 2026-03-30T11:26:28.403Z
      type: summary
    - text: Added /next slash command, assist draft/bug CLI commands with signal-based chaining. Extracted writeSignal/readSignal helpers, moved getSignalPath to writeSignal.ts to avoid circular deps. On /next signal, Claude is terminated and assist next runs automatically.
      phase: 1
      timestamp: 2026-03-30T11:33:48.671Z
      type: summary
- type: story
  name: Global config support for sync auto-confirm
  description: Add a sync.autoConfirm config key (global-only) that controls whether assist sync skips confirmation prompts. Extend assist config set with a --global flag to write to ~/.assist.yml. Wire assist update and assist sync to read this setting.
  acceptanceCriteria:
    - assist config set --global sync.autoConfirm true writes to ~/.assist.yml
    - assist sync reads sync.autoConfirm from config and skips prompts when true
    - assist update no longer hardcodes --yes; it calls assist sync and lets the config decide
    - assist config set sync.autoConfirm true (without --global) errors or warns since it is a global-only key
    - Default is false (existing behavior preserved)
  plan:
    - name: Schema and global config set
      tasks:
        - task: Add sync schema to assistConfigSchema with autoConfirm boolean defaulting to false
          verify: npx vitest run src/shared/types
        - task: Add --global flag to assist config set that writes via saveGlobalConfig
          verify: npx vitest run src/commands/config
        - task: Add tests for global config set
          verify: npx vitest run src/commands/config
    - name: Wire sync and update
      tasks:
        - task: Update sync to read sync.autoConfirm from config and pass it as the yes option
          verify: npx vitest run src/commands/sync
        - task: Update update to call assist sync without --yes, relying on config
          verify: npx vitest run src/commands/update
        - task: Add tests for both paths
          verify: npx vitest run src/commands/sync src/commands/update
  id: 27
  status: todo
