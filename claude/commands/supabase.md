---
description: Drive the Supabase CLI
---

The user wants to work with Supabase. Use the official `supabase` CLI directly.

Run `supabase help` (or `supabase <command> --help`) to discover the available commands and flags, then run what the task needs. Display the results, summarising if the output is large.
