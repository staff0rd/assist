import type { Session, SessionInfo } from "./createSession";

export function toSessionInfo({
	id,
	name,
	title,
	subtitle,
	commandType,
	status,
	startedAt,
	runningMs,
	runningSince,
	runName,
	runArgs,
	assistArgs,
	cwd,
	restored,
	error,
	activity,
	autoRun,
	autoAdvance,
	starred,
	usedPct,
	design,
}: Session): SessionInfo {
	return {
		id,
		name,
		title,
		subtitle,
		commandType,
		status,
		startedAt,
		runningMs,
		runningSince,
		runName,
		runArgs,
		assistArgs,
		cwd,
		restored,
		error,
		activity,
		autoRun,
		autoAdvance,
		starred,
		usedPct,
		design,
	};
}
