import type { Session, SessionInfo } from "./createSession";

export function toSessionInfo({
	id,
	name,
	title,
	subtitle,
	commandType,
	harness,
	status,
	startedAt,
	runningMs,
	runningSince,
	runName,
	runArgs,
	server,
	serverPort,
	assistArgs,
	cwd,
	restored,
	error,
	errorOutput,
	activity,
	autoRun,
	autoAdvance,
	starred,
	usedPct,
	design,
}: Session): SessionInfo {
	return {
		id,
		name,
		title,
		subtitle,
		commandType,
		harness,
		status,
		startedAt,
		runningMs,
		runningSince,
		runName,
		runArgs,
		server,
		port: serverPort,
		assistArgs,
		cwd,
		restored,
		error,
		errorOutput,
		activity,
		autoRun,
		autoAdvance,
		starred,
		usedPct,
		design,
	};
}
